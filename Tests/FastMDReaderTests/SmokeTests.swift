import XCTest
@testable import FastMDReader

final class SmokeTests: XCTestCase {
    func testAppTargetLinks() {
        // Placeholder proving the test target builds against the app target.
        // Real logic tests arrive in Task 3+.
        XCTAssertTrue(true)
    }
}
